# Loaded by docker compose for the backend service (no real secrets — override locally).
JWT_SECRET=referx_docker_dev_jwt_change_me
# 32-byte AES key (base64). For production, replace with a strong secret from `openssl rand -base64 32`.
PAYMENT_ENCRYPTION_KEY=vmfzNPOyUgCD2K3kOxxsCzWCh0Gi0b8sBgBdUVFPJlc=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
